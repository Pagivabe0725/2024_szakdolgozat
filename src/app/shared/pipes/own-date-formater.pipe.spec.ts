import { OwnDateFormaterPipe } from './own-date-formater.pipe';

describe('OwnDateFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new OwnDateFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
